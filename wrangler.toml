# Cloudflare Workers 配置
name = "product-consignment-studio"
main = "src/worker/index.ts"
compatibility_date = "2024-01-01"

# D1 数据库绑定
[[d1_databases]]
binding = "DB"  # 在 Worker 中通过 env.DB 访问
database_name = "hdin-consignment-studio"
database_id = "f1bde843-1e55-449f-9b29-16e4b9564467"

# 开发环境配置
[dev]
port = 8787
local_protocol = "http"

# 部署到 Cloudflare Pages 时的配置 (可选)
# [site]
# bucket = "./dist"
